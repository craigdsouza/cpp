what are the three steps in compilation?
- pre-processing - handling of #include and #define
- compiling - conversion to assembly and then machine code, obj files
- linking - links obj files with std library and external libraries to make an executable file

C++ is a strict language. 
This makes it more robust in production because it detects in advance errors that could potentially have occurred.