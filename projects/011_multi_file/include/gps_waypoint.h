// Exercise 1 — Header file for GPSWaypoint
// This file DECLARES what GPSWaypoint is — no implementation goes here.
//
// Step 1: add #pragma once at the very top (prevents multiple-inclusion errors)

// Step 2: include <string> (needed for std::string in the struct)

// Step 3: declare the struct
// struct GPSWaypoint {
//     float lat;
//     float lon;
//     std::string name;
//
//     // Constructor declaration — just the signature, no body
//     // GPSWaypoint(float lat, float lon, const std::string& name);
// };
